project('unflobtactical', 'cpp')

catch_inc_dir = include_directories('external')
# log_dep = dependency('liblog')
e = executable(
  'unit-tests', 
  'tests/main.cpp', 
  # dependencies: log_dep
)
test('test', e)
